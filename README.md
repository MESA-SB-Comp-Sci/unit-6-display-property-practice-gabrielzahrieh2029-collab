# Display Properties — Guided Practice

**Name:** ___________________________
**Date:** ___________________________

---

## Instructions

Open `style.css` and find each task below. Change the `display` value to complete each one. Check your work in the browser after each task.

---

## Task 1

The nav links are currently stacked on top of each other.

Change the `display` property on `.nav-link` so they sit side by side.

---

## Task 2

The cards are also stacked on top of each other.

Change the `display` property on `.card` so they sit side by side.

---

## Task 3

The credits in the footer are currently sitting side by side.

Change the `display` property on `.footer-credit` so they stack on top of each other.

---

## Reflection

Answer the questions below after completing all three tasks.

**1. Which display value did you use for Tasks 1 and 2? Why does that value make elements sit side by side?**

&nbsp;

&nbsp;

**2. Which display value did you use for Task 3? Why does that value make elements stack?**

&nbsp;

&nbsp;

**3. What is the difference between `display: block` and `display: inline-block`?**

&nbsp;

&nbsp;