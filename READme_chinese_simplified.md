# 显示属性 — 引导练习
# Display Properties — Guided Practice

**姓名 / Name:** ___________________________
**日期 / Date:** ___________________________

---

## 说明 / Instructions

打开 `style.css`，找到下面每个任务。修改 `display` 的值来完成每个任务。每完成一个任务后，在浏览器中检查你的效果。

Open `style.css` and find each task below. Change the `display` value to complete each one. Check your work in the browser after each task.

---

## 任务 1 / Task 1

导航链接目前是叠放在一起的。

The nav links are currently stacked on top of each other.

将 `.nav-link` 的 `display` 属性改为让它们并排显示。

Change the `display` property on `.nav-link` so they sit side by side.

---

## 任务 2 / Task 2

卡片也是叠放在一起的。

The cards are also stacked on top of each other.

将 `.card` 的 `display` 属性改为让它们并排显示。

Change the `display` property on `.card` so they sit side by side.

---

## 任务 3 / Task 3

页脚中的版权信息目前是并排显示的。

The credits in the footer are currently sitting side by side.

将 `.footer-credit` 的 `display` 属性改为让它们上下叠放。

Change the `display` property on `.footer-credit` so they stack on top of each other.

---

## 反思 / Reflection

完成所有三个任务后回答以下问题。

Answer the questions below after completing all three tasks.

**1. 你在任务 1 和任务 2 中使用了哪个 display 值？为什么那个值能让元素并排显示？**

**Which display value did you use for Tasks 1 and 2? Why does that value make elements sit side by side?**

&nbsp;

&nbsp;

**2. 你在任务 3 中使用了哪个 display 值？为什么那个值能让元素上下叠放？**

**Which display value did you use for Task 3? Why does that value make elements stack?**

&nbsp;

&nbsp;

**3. `display: block` 和 `display: inline-block` 有什么区别？**

**What is the difference between `display: block` and `display: inline-block`?**

&nbsp;

&nbsp;